# Dependencies
node_modules/

# Next.js build output
.next/
out/
build/

# Secrets & environment files — NEVER commit these.
# All real API keys live in .env.local (locally) or in Vercel/Claude
# environment settings (deployed/remote). See SETUP.md.
.env
.env.local
.env.*.local
.env.development.local
.env.test.local
.env.production.local

# Vercel
.vercel

# Supabase local dev
supabase/.temp
supabase/.branches

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Logs & OS junk
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
